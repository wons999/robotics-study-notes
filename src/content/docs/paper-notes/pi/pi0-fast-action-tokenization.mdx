---
title: π0-FAST
description: FAST action tokenizer와 π0-FAST autoregressive VLA 구현을 정리한 Survey.
sidebar:
  hidden: true
---

import SectionFeedback from '../../../../components/SectionFeedback.astro';
import PublicImage from '../../../../components/PublicImage.astro';

# π0-FAST

<SectionFeedback page="π0-FAST" section="메타데이터" />

## 메타데이터

| 항목 | 내용 |
| --- | --- |
| 논문 | FAST: Efficient Action Tokenization for Vision-Language-Action Models |
| 저자/그룹 | Karl Pertsch, Kyle Stachowicz, Brian Ichter, Danny Driess, Suraj Nair, Quan Vuong, Oier Mees, Chelsea Finn, Sergey Levine |
| 공개 | arXiv 2025-01-16 |
| 링크 | https://arxiv.org/abs/2501.09747, https://www.pi.website/research/fast |
| 코드/모델 | https://github.com/Physical-Intelligence/openpi, `gs://openpi-assets/checkpoints/pi0_fast_base` |
| 태그 | VLA, action tokenization, DCT, autoregressive policy, high-frequency control |

## 한 줄 요약

π0-FAST는 continuous robot action sequence를 DCT 기반 frequency-space token으로 압축해 autoregressive VLA가 high-frequency dexterous action을 학습할 수 있게 만든 PI 계열의 token-action route다.

## Official Visual Evidence

| FAST qualitative task example |  |  |
| --- | --- | --- |
| <PublicImage src="/pi/fast-fold-shirt-1.jpg" alt="FAST folding example 1" /> | <PublicImage src="/pi/fast-fold-shirt-2.jpg" alt="FAST folding example 2" /> | <PublicImage src="/pi/fast-fold-shirt-3.jpg" alt="FAST folding example 3" /> |

공식 FAST article이 π0/π0-FAST 계열의 high-dexterity task 예시로 사용하는 folding visual이다. FAST의 핵심 figure는 DCT/BPE tokenization diagram이 React-rendered graphic이므로, 본문에서는 공식 설명의 token 길이와 압축률을 표로 전사하고 대표 qualitative visual을 함께 둔다.

## Source/Image Inventory

| Source | Local asset / code path | 본문 반영 위치 | 읽을 때 주의할 점 |
| --- | --- | --- | --- |
| arXiv PDF `2501.09747` | PDF 직접 확인, 별도 crop 미추출 | PDF Figure/Table Inventory, Experiments | FAST는 논문 figure가 tokenization/result 중심이다. 현재 문서는 공식 article visual을 쓰되, 수치와 실험 구조는 PDF 기준으로 대조한다. |
| PI website FAST article | `/pi/fast-fold-shirt-1.jpg`, `/pi/fast-fold-shirt-2.jpg`, `/pi/fast-fold-shirt-3.jpg` | Official Visual Evidence | qualitative folding example이며 정량 result figure를 대체하지 않는다. |
| Official OpenPI repository | `src/openpi/models/pi0_fast.py`, `src/openpi/models/tokenizer.py`, `src/openpi/training/config.py`, `src/openpi/transforms.py`, `src/openpi/models/model.py` | OpenPI Config Snapshot, Code-Level Check | public `π0-FAST`는 JAX path 중심이다. README의 PyTorch support caveat와 분리해서 읽는다. |

## PDF Figure/Table Inventory

이번 pass에서는 FAST PDF를 직접 확보하고 text extraction으로 Figure/Table inventory를 확인했다. 본문은 이미 공식 article visual을 사용하고 있으므로, 이번 커밋에서는 PDF crop을 새로 늘리지 않고 어떤 figure/table이 어떤 주장에 대응되는지 추적 가능하게 남긴다.

| 원문 항목 | Survey 반영 위치 | 읽어야 할 핵심 |
| --- | --- | --- |
| Figure 1 | 한 줄 요약, Family 위치 | FAST가 action sequence를 language-token route로 넣는 전체 아이디어 |
| Figure 2 | 문제의식 | standard discretization이 high-frequency dexterous task에서 깨지는 이유 |
| Figure 3 | Experiments | sampling rate가 올라갈 때 naive tokenization prediction error가 어떻게 나빠지는지 |
| Figure 4 | Method | normalized action chunk -> DCT -> quantization -> BPE tokenization pipeline |
| Figure 5 | Experiments | evaluation environments: table bussing, shirt folding, grocery bagging 등 |
| Table I | Method/Experiments | 평균 token count와 compression-reconstruction trade-off 비교 |
| Figure 6 | Experiments | tokenization approach별 policy performance 비교 |
| Figure 7 | Experiments | FAST policy의 real-world/university setup qualitative evaluation |
| Figure 8 / Table III | Dataset | universal tokenizer 평가 dataset inventory |
| Figure 9 | π0와 비교 | diffusion π0와 autoregressive FAST route의 performance/convergence 비교 |
| Figure 10 | Official Visual Evidence | laundry folding rollout qualitative example |
| Figure 11 / Figure 15 | Experiments | π0-FAST와 diffusion π0, compute-matched π0 비교 |
| Figure 12 | Experiments | compression-reconstruction trade-off appendix result |
| Figure 13 / Table II | Appendix | DROID evaluation initial configuration/task list |
| Figure 14 | Appendix | quantitative DROID evaluation setup |

## Family 안에서의 위치

`π0`가 continuous action chunk를 flow matching으로 직접 생성한다면, `π0-FAST`는 action chunk를 token sequence로 바꾸어 language-model style next-token prediction을 쓴다. FAST paper의 핵심은 “기존 per-dimension/per-timestep discretization은 high-frequency dexterous skill에서 깨지지만, frequency-space compression은 fidelity와 token budget 사이 trade-off를 훨씬 낫게 만든다”는 것이다.

| 비교 축 | π0 | π0-FAST |
| --- | --- | --- |
| output | continuous action chunk | discrete action tokens |
| objective | flow matching MSE | autoregressive CE |
| inference cost | multiple denoising steps | left-to-right token decoding |
| 장점 | precise multimodal continuous action | VLM language modeling route와 자연스럽고 training time 감소 |
| 단점 | sampling 반복 필요 | tokenization fidelity와 decoding length가 병목 |

## 문제의식

기존 VLA의 action tokenization은 보통 action dimension과 timestep을 각각 binning한다. 이 방식은 낮은 주파수/낮은 DoF task에서는 동작할 수 있지만, bimanual dexterity나 50 Hz action sequence에서는 token 수가 커지고 reconstruction error가 task failure로 이어진다.

FAST는 action sequence를 time domain 그대로 token화하지 않는다. action trajectory가 시간적으로 smooth하고 저주파 성분이 많다는 점을 이용해 frequency domain에서 압축한다. 공식 pi.website 글은 action chunk가 crude system에서는 3-5 actions, high-frequency dexterous robot에서는 20-50 actions 범위가 될 수 있고, 기존 dimension/timestep binning은 이 구간에서 빠르게 깨진다고 설명한다.

## Method

### FAST Tokenization

FAST의 절차는 다음처럼 읽을 수 있다.

1. action sequence를 fixed-length chunk로 본다.
2. action dimension별 sequence를 frequency basis로 변환한다.
3. 중요한 frequency coefficient를 quantize/tokenize한다.
4. BPE류 compression을 적용해 variable-length token sequence를 만든다.
5. autoregressive VLA는 image/language/state prompt 뒤에 FAST action tokens를 next-token objective로 예측한다.
6. inference 후 tokenizer가 token sequence를 다시 continuous action chunk로 복원한다.

공식 글 기준 FAST token sequence는 보통 action chunk당 30-60 tokens로 압축된다. 이는 prior action tokenization 대비 약 10x compression이라고 설명된다. FAST+는 universal robot action tokenizer로, 1M real robot action sequences로 훈련된 버전을 공개했다. 다양한 action space/control frequency를 흡수하기 위해 action을 32 dimension으로 padding하는 strategy가 appendix에 나온다.

### π0-FAST Model

openpi 구현에서 `Pi0FAST`는 flow head를 쓰지 않는다.

| 구성 | 구현 내용 |
| --- | --- |
| config | `Pi0FASTConfig`, 기본 `action_dim=32`, `action_horizon=32`, `max_token_len=250` |
| image encoder | SigLIP `So400m/14`, 224 x 224 image |
| language/action backbone | `gemma_fast` PaliGemma-style autoregressive LLM |
| input transform | `TokenizeFASTInputs`가 prompt, state, optional actions를 token sequence로 변환 |
| loss | target token의 next-token cross entropy |
| output transform | `ExtractFASTActions`가 generated tokens를 continuous actions로 복원 |

openpi `Pi0FAST.compute_loss`는 input token embeddings를 만들고, `token_loss_mask`가 켜진 target token에 대해서만 CE loss를 계산한다. `sample_actions`는 prefix를 KV cache로 prefill한 뒤 token을 autoregressive로 생성하고, EOS 또는 max decoding step에서 멈춘다.

## OpenPI Config Snapshot

아래 값은 공식 `Physical-Intelligence/openpi` repository의 `Pi0FASTConfig`와 `ModelTransformFactory` 기준이다.

| 항목 | OpenPI 기본값 | 의미 |
| --- | --- | --- |
| `model_type` | `ModelType.PI0_FAST` | `Pi0FASTConfig.model_type`이 반환하는 모델 타입 |
| `dtype` | `bfloat16` | PaliGemma/Gemma-Fast embedding/cache dtype |
| `paligemma_variant` | `gemma_2b` | FAST용 autoregressive backbone variant |
| `action_dim` | 32 | base π0-FAST의 padded action dimension. downstream config에서 override 가능 |
| `action_horizon` | 32 | base π0-FAST action chunk length. π0 flow model default 50과 다르다. |
| `max_token_len` | 250 | prompt + discretized state + FAST action tokens를 담는 non-image token budget |
| tokenizer | `FASTTokenizer(max_token_len)` | default tokenizer path는 `physical-intelligence/fast` |
| image transform | `ResizeImages(224, 224)` | public OpenPI input preprocessing |
| output transform | `ExtractFASTActions(tokenizer, action_horizon, action_dim)` | generated token sequence를 continuous action chunk로 복원한다. |

OpenPI `inputs_spec` 기준 대표 shape는 다음과 같다.

| 입력/출력 | Shape |
| --- | --- |
| `base_0_rgb`, `base_1_rgb`, `wrist_0_rgb` | `[B, 224, 224, 3]` |
| image masks | `[B]` per camera |
| state | `[B, action_dim=32]` |
| tokenized prompt/action sequence | `[B, max_token_len=250]` |
| `token_ar_mask` | `[B, max_token_len=250]` |
| `token_loss_mask` | `[B, max_token_len=250]` |
| decoded actions | `[B, action_horizon=32, action_dim=32]` |

Downstream config는 이 기본값을 자주 override한다. 예를 들어 `pi0_fast_libero`는 `action_dim=7`, `action_horizon=10`, `max_token_len=180`이고, `pi0_fast_full_droid_finetune`은 `action_dim=8`, `action_horizon=16`, `max_token_len=180`, `batch_size=256`, `num_train_steps=100_000`이다. 따라서 base model config와 benchmark/fine-tuning config를 같은 값처럼 쓰면 안 된다.

## FAST Token Contract

`FASTTokenizer` 구현 기준으로 token sequence는 다음 구조를 갖는다.

| 단계 | 구현상 의미 |
| --- | --- |
| prompt cleaning | prompt를 lowercase/strip하고 `_`를 space로 바꾼다. |
| state discretization | normalized state를 `[-1, 1]` 범위의 256 bins로 discretize한다. |
| prefix string | `Task: PROMPT, State: STATE_STR;\n` 형식으로 만든다. |
| prefix mask | prefix는 bidirectional attention을 쓰도록 `token_ar_mask=0`이 된다. |
| action tokenization | `physical-intelligence/fast` processor가 action chunk를 FAST token으로 바꾼다. |
| Paligemma vocab mapping | FAST action token을 PaliGemma vocab 끝쪽 token으로 mapping한다. 마지막 128 special token은 skip한다. |
| postfix string | `Action: FAST_TOKENS|EOS` 구조다. |
| loss mask | prefix는 false, action postfix는 true다. CE loss는 action token 구간에만 걸린다. |
| padding/truncation | token 길이가 `max_token_len`보다 짧으면 padding하고, 길면 truncation warning을 낸다. |
| action extraction | generated token을 decode해 `Action:`과 `|` 사이 token만 FAST processor로 continuous action에 복원한다. |

이 계약 때문에 π0-FAST의 “모델 출력”은 곧바로 robot action이 아니다. 먼저 PaliGemma token id sequence가 나오고, `ExtractFASTActions`가 그 token을 FAST tokenizer로 decode해야 `[H, action_dim]` continuous action chunk가 된다.

## Dataset

FAST tokenizer appendix는 universal tokenizer training mixture를 dataset/morphology/action space/control frequency/mixture weight로 공개한다.

| 데이터 축 | 내용 |
| --- | --- |
| morphology | single arm, bi-manual, mobile |
| action space | joint, end-effector, camera-frame end-effector |
| frequency | 5 Hz, 15 Hz, 20 Hz, 50 Hz, mixed |
| 포함 dataset | PI internal robot datasets, ALOHA, DROID, Bridge V2, Open X-Embodiment |
| preprocessing | action은 tokenizer 전 32 dimension으로 padding |

이 설계의 목적은 tokenizer 자체가 특정 robot action dimension에 과적합하지 않게 하는 것이다. action representation이 다른 dataset도 같은 tokenizer vocabulary를 공유하게 만든다.

## Training Recipe

| 항목 | FAST paper / openpi 기준 |
| --- | --- |
| tokenizer | DCT/frequency-space compression + token vocabulary |
| policy backbone | FAST paper는 π0와 OpenVLA backbone 실험, openpi는 `Pi0FAST` |
| image input | task별 2개 또는 3개 image, openpi spec은 base/wrist views |
| state input | openpi FAST path는 state를 prompt/action token sequence 쪽으로 넣는다. |
| objective | autoregressive next-token CE |
| fine-tuning | openpi `pi0_fast_libero`, `pi0_fast_droid`, `pi0_fast_full_droid_finetune` configs 제공 |
| checkpoint | `pi0_fast_base`, `pi0_fast_droid` |

openpi DROID full fine-tuning config는 `action_dim=8`, `action_horizon=16`, `max_token_len=180`, `num_train_steps=100_000`, `batch_size=256`으로 되어 있고, 주석은 8 x H100에서 약 2일 정도를 언급한다. 이 값은 paper 전체 training recipe가 아니라 공개 repo의 DROID fine-tuning 예시다.

## Experiments and Results

FAST paper의 실험은 tokenizer 자체와 VLA policy 성능을 함께 본다.

| 실험 | 질문 | 결론 |
| --- | --- | --- |
| compression-reconstruction | 같은 token budget에서 action reconstruction fidelity가 충분한가 | FAST는 높은 fidelity 영역에서 naive discretization이나 일부 VQ-style tokenizer보다 유리하다. |
| dexterous skill learning | high-frequency task에서 AR VLA가 action을 배울 수 있는가 | 기존 discretization은 실패하는 setting에서도 FAST는 학습 가능하게 만든다. |
| π0와 결합 | flow VLA와 비교해 성능/효율이 어떤가 | paper abstract는 10k hours robot data scale에서 diffusion VLA와 성능을 맞추면서 training time을 up to 5x 줄였다고 요약한다. |
| universal tokenizer | action space와 control frequency가 다른 robot에 일반화되는가 | 1M real robot trajectories 기반 FAST+를 black-box tokenizer로 제시한다. |
| DROID zero-shot | full DROID dataset으로 새 환경 language command를 따를 수 있는가 | pi.website article은 UC Berkeley, Stanford, University of Washington 협력 환경에서 out-of-box simple manipulation을 수행했다고 설명한다. |
| standard discretization ablation | 기존 binning route가 dexterous suite를 풀 수 있는가 | 공식 article은 standard discretization methods가 experimental repertoire의 dexterous tasks를 하나도 해결하지 못했다고 말한다. |

Appendix B의 compression-reconstruction trade-off는 이 논문의 핵심이다. tokenizer는 단순히 “압축률이 높다”가 아니라, fine-grained control에 필요한 reconstruction fidelity가 충분한 영역에서 token 수를 줄여야 한다. FAST는 low-fidelity 영역에서는 VQ류가 더 효율적일 수 있지만, high-fidelity dexterous control 영역에서는 scaling이 더 좋다고 해석한다.

π0-FAST는 π0와 같은 backbone 및 training dataset을 쓰는 autoregressive generalist policy로 소개된다. 공식 article의 실험 메시지는 세 가지다.

| 주장 | 근거 |
| --- | --- |
| dexterity 유지 | folding laundry, bussing tables, packing grocery bags 같은 high-dexterity task를 해결한다. |
| 학습 효율 | π0 대비 up to 5x faster training을 보고한다. |
| language route 강화 | action도 token으로 다루므로 VLM/LM pretraining route와 더 자연스럽게 이어지고 language instruction following이 개선될 수 있다고 설명한다. |

OpenPI article은 별도 caveat도 제공한다. `π0-FAST base`는 language following이 somewhat better인 반면, PI 경험상 inference cost가 about 4-5x higher라고 설명한다. 따라서 “훈련은 빠르지만 runtime은 항상 싸다”라고 쓰면 틀린다.

## Code-Level Check

| 경로 | 확인 내용 |
| --- | --- |
| `src/openpi/models/pi0_fast.py` | `Pi0FASTConfig`, `Pi0FAST.compute_loss`, AR decoding loop |
| `src/openpi/transforms.py` | `TokenizeFASTInputs`, `ExtractFASTActions` |
| `src/openpi/training/config.py` | `pi0_fast_droid`, `pi0_fast_libero`, `pi0_fast_full_droid_finetune` |
| `src/openpi/models/model.py` | `ModelType.PI0_FAST`, FAST-specific `token_ar_mask`, `token_loss_mask` |
| `src/openpi/policies/policy_config.py` | generated tokens를 policy output transform에서 action으로 복원 |

구현에서 특히 중요한 점은 `Pi0FAST.sample_actions`의 출력이 처음에는 token sequence라는 것이다. 실제 continuous action은 policy output transform의 `ExtractFASTActions`를 거친 뒤 얻어진다. 따라서 raw model output과 robot action을 혼동하면 안 된다.

### OpenPI Loss / Decoding Contract

| 단계 | 공개 코드 기준 |
| --- | --- |
| input embedding | three image streams와 tokenized prompt/action sequence를 embedding한 뒤 concat한다. |
| attention mask | image/prefix token은 bidirectional, action postfix token은 causal AR mask를 쓴다. |
| CE target | `tokenized_prompt[:, 1:]`를 next-token target으로 사용한다. |
| loss mask | `token_loss_mask[:, 1:]`가 true인 action postfix token에만 CE loss를 건다. |
| memory saving | full vocab logits는 target token 구간에 대해서만 decode한다. |
| inference prefill | prefix token을 KV cache에 먼저 채운다. |
| decoding | argmax 또는 temperature sampling으로 left-to-right token을 생성한다. |
| stop condition | batch 전체가 EOS를 내거나 `max_decoding_steps=256`에 도달하면 멈춘다. |
| output | raw `output_tokens`; continuous action은 transform에서 별도 추출한다. |

## 한계와 주의

- tokenization이 좋아져도 AR decoding length가 길어지면 latency가 증가한다.
- FAST tokenizer의 reconstruction fidelity는 action chunk length, action dimension, control frequency에 따라 달라진다.
- flow matching route와 비교할 때 성능, language-following, training cost를 같은 task/data/robot 조건에서 비교해야 한다.
- openpi PyTorch support README는 현재 `π0-FAST`가 PyTorch implementation feature list에서 제외되어 있다고 설명한다. JAX path 중심으로 보는 것이 안전하다.

## Citation Trail

| 연결 논문 | 연결 이유 |
| --- | --- |
| RT-2 / OpenVLA | discrete action token VLA의 기존 route |
| π0 | continuous flow action route와 직접 비교 |
| DCT / compression tokenizer | action sequence를 frequency-space로 압축하는 기반 |
| Open X-Embodiment, DROID, Bridge | universal tokenizer data mixture |
| BEAST / later action tokenizer papers | action tokenization 계열 후속 비교 |

## 검증 메모

| Pass | 결과 |
| --- | --- |
| 1. 구조/coverage | FAST tokenizer, π0-FAST model, PDF figure/table inventory, dataset mixture, experiments, code path를 분리했다. |
| 2. 사실/수치 | FAST PDF와 pi.website FAST article의 30-60 tokens/chunk, 10x compression, 1M sequences, 5x training speed, DROID zero-shot, OpenPI 4-5x inference caveat를 대조했다. |
| 3. 코드 구현 | `pi0_fast.py`, `tokenizer.py`, `transforms.py`, `training/config.py`, `model.py`를 확인해 `action_dim=32`, `action_horizon=32`, `max_token_len=250`, FAST token mapping, CE loss mask, AR decoding path를 반영했다. |
| 4. 해석/연결 | π0 flow route, OpenVLA/RT-2 token route, PI family 안의 위치를 정리했다. |
| 5. 문서 품질/빌드 | `npm run build`와 GitHub Pages base path build로 MDX와 route 생성을 확인한다. |
